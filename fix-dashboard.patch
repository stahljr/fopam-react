--- a/src/Dashboard.js
+++ b/src/Dashboard.js
@@ -1917,7 +1917,9 @@
       setSnackbar({
         open: true,
         severity: fail ? 'warning' : 'success',
-        message: `WhatsApp enviado: ${ok} sucesso${ok!==1?'s':''}${fail?`, ${fail} falha${fail!==1?'s':''}`:''}.`
+        message: `WhatsApp enviado: ${ok} sucesso${ok !== 1 ? 's' : ''}` +
+                 (fail ? `, ${fail} falha${fail !== 1 ? 's' : ''}` : '') +
+                 '.'
       });
       setMassDialogOpen(false);
     } catch (err) {
